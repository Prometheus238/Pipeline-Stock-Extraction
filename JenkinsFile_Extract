pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            steps {
                script {
                    sh 'pip install yfinance --break-system-packages'
                    sh 'pip3 install json --break-system-packages'
                    sh 'pip3 install psycopg2 --break-system-packages'
                    sh 'pip3 install datetime --break-system-packages'
                }
            }
        }

        stage('Extract Data') {
            steps {
                script {
                    sh 'python3 extract_data.py'
                }
            }
        }
    }

    post {
        success {
            echo 'Job completed successfully'
        }
        failure {
            echo 'Job failed'
        }
    }
}
