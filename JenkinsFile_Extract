pipeline {
    agent any

    stages {       
        stage('Extract Data') {
            steps {
                script {
                    sh 'python3 extract_data.py'
                }
            }
        }
    }

    post {
        success {
            echo 'Job completed successfully'
        }
        failure {
            echo 'Job failed'
        }
    }
}
